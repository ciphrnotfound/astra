package main\n\nimport (\n    "encoding/json"\n    "log"\n    "net/http"\n)\n\n// CalculatorResponse represents the response of the calculator API.\ntype CalculatorResponse struct {\n    Result float64 `json:"result"`\n}\n\n// CalculateHandler handles the calculation endpoint.\nfunc CalculateHandler(w http.ResponseWriter, r *http.Request) {\n    // Parse the query parameters from the request.\n    var operation string\n    var num1, num2 float64\n    err := json.NewDecoder(r.Body).Decode(&operation)\n    if err != nil {\n        log.Println("Error decoding JSON:", err)\n        return\n    }\n\n    switch operation {\n    case "+":\n        result := num1 + num2\n        json.NewEncoder(w).Encode(CalculatorResponse{Result: result})\n    case "-":\n        result := num1 - num2\n        json.NewEncoder(w).Encode(CalculatorResponse{Result: result})\n    case "*":\n        result := num1 * num2\n        json.NewEncoder(w).Encode(CalculatorResponse{Result: result})\n    case "/":\n        if num2 == 0 {\n            log.Println("Error: Division by zero.")\n            return\n        }\n        result := num1 / num2\n        json.NewEncoder(w).Encode(CalculatorResponse{Result: result})\n    default:\n        log.Println("Error: Operation not supported.")\n        return\n    }\n}\n\nfunc main() {\n    // Serve the calculator API at the root URL.\n    http.HandleFunc("/", CalculateHandler)\n    log.Println("Calculator API server started.")\n    http.ListenAndServe("":8080, nil)\n