function greet(name: string): string {
    return `Hello, ${name}`;
}

function add(a: number, b: number): number {
    return a + b;
}

function formatUser(id: number, username: string): string {
    return `${id}:${username.toLowerCase()}`;
}