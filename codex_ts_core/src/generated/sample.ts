
export function greet(name: string): string {
  // format!("Hello, {}", name)
}


export function add(a: number, b: number): number {
  // a + b
}


export function format_user(id: number, username: string): string {
  // format!("{}:{}", id, username.to_lowercase())
}

